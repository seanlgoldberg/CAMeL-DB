README:
========

NOTE: All resources are in src/postQuestions. This is a constant in AMTWorkflow.
 
	1)AMT Workflow has main class

	Run as : AMTWorkflow out.csv hitmap.csv

	This takes the output schema generated by CreateQuestionTemplate and writes relevant hitID's to hitmap.csv
	Do not delete either file. Only append new citations to out.csv.


	2)Getting Results
	Run as: HITProcessor hitmap.csv
	
	This takes unique HITID's from hitmap.csv and gets results once assignment available for the HIT becomes zero.
	Output is an XML file with the answer. This filename <HITID>.xml, where <HITID> is the corresponding HITID. 